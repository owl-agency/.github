name: CodeQL
on:
  push:
    branches: [main]
  schedule:
    - cron: '0 0 * * 0'

jobs:
  analyze:
    uses: github/codeql-action/.github/workflows/codeql.yml@v2
    with:
      languages: javascript
